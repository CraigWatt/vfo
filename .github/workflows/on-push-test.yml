name: Validate (PR + main)

on:
  pull_request:
    branches:
      - "main"
  push:
    branches:
      - "main"

jobs:
  build:
    runs-on: macos-latest

    steps:
      - uses: actions/checkout@v6

      - name: add homebrew to PATH
        run: |
          if [ -d /opt/homebrew/bin ]; then
            echo "/opt/homebrew/bin" >> "$GITHUB_PATH"
          fi
          if [ -d /usr/local/bin ]; then
            echo "/usr/local/bin" >> "$GITHUB_PATH"
          fi

      - name: verify brew is installed
        run: brew -v

      - name: install CI dependencies
        run: |
          if ! brew list cmocka >/dev/null 2>&1; then
            brew install cmocka
          fi
          if ! command -v ffmpeg >/dev/null 2>&1; then
            brew install ffmpeg
          fi
          if ! command -v mkvmerge >/dev/null 2>&1; then
            brew install mkvtoolnix
          fi

      - name: build + unit + hosted synthetic e2e smoke
        env:
          VFO_E2E_ASSET_MODE: synthetic
          VFO_E2E_MAX_SEEDS: "1"
        run: make ci

      - name: upload e2e debug artifacts on failure
        if: failure()
        uses: actions/upload-artifact@v7
        with:
          name: e2e-debug-${{ github.sha }}
          path: tests/e2e/.tmp/
          if-no-files-found: ignore
          retention-days: 7

      - name: upload e2e toolchain reports
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: e2e-toolchain-${{ github.sha }}
          path: tests/e2e/.reports/latest/
          if-no-files-found: ignore
          retention-days: 7

      - name: clean test artifacts
        if: always()
        run: make clean_tests

      - name: clean build artifacts
        if: always()
        run: make clean
